{
	"scripts": {
		"build": "pnpm run build:types && pnpm run build:core",
		"build:types": "pnpm --filter @lmoulanier/elements-writable-properties-types run build",
		"build:core": "pnpm --filter @lmoulanier/el run build"
	}
}
